[project]
name = "dummy-claude-code"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "openai>=2.15.0",
    "groq>=0.11.0",
    "python-dotenv>=1.0.0",
]

[project.scripts]
dummy-claude-code = "app.main:main"

[tool.uv]
package = true
